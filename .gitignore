# OneDrive download artifacts
OneDrive_*.zip

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
/build/
/dist/

# Disposable repository-local validation workspaces.
/.agent-*-tests/
/mm-test-*/
/mm-feedback-source-*/
/mm-feedback-target-*/

# OS noise
Thumbs.db
.DS_Store

# Derived, disposable state (principle P1: reconstructable from canonical Markdown).
# Tracked deliberately? No — packs and telemetry are rebuildable; keeping them out of
# history proves nothing canonical depends on them.
outputs/context/*.md
_meta/recall-log.tsv
_meta/session-state/
